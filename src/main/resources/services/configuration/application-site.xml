<?xml version="1.0"?>
<?xml-stylesheet type="text/xsl" href="configuration.xsl"?>

<configuration>

   <!-- Managed Application properties. On service start all properties defined here are converted into spring boot applicatoin.properties 
    file stored in: /opt/{your application name}/application.properties.
    
    You can expose either some of the predefined common application properties: 
    http://docs.spring.io/spring-boot/docs/1.3.0.BUILD-SNAPSHOT/reference/htmlsingle/#common-application-properties or application 
    specific properties.
    
    In addition to the predefined properties here you can use the Ambari 'Configs\Custom [app-name]-site' menu to add additional properties at runtime.  
    
    Note that the applicaiton-site.xml is renamed into [app-name]-site.xml at compile time.  
  -->
  <property>
    <name>server.port</name>
    <value>8082</value>
    <description>Port the rest server is running on. Default value: 8082.</description>
  </property>

  <property>
    <name>security.user.password</name>
    <value>changeme</value>
    <description>Web server password</description>
  </property>

</configuration>